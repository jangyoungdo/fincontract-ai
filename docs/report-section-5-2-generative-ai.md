## 5-2. 생성형 AI 모델 적용 방안

본 서비스는 생성형 AI가 계약의 위험 여부를 단독으로 결정하는 구조가 아니라, 개발 단계와 서비스 운영 단계에서 모델의 역할을 분리하고 각 결과를 결정론적 코드로 통제하는 구조로 설계하였다. 개발 단계에서는 Claude Code와 Codex를 AI 코딩 에이전트로 활용하였다. 도메인 담당자가 법령·판례 검토를 바탕으로 위험 유형, 정상 예외, 기대 탐지 결과를 정의하면, 두 에이전트가 이를 규칙 엔진, 검색 파이프라인, 자동 테스트와 기술 문서로 구체화하였다. 이 과정의 핵심은 AI가 임의로 기능을 만드는 것이 아니라 도메인 지식을 실행 가능한 명세와 회귀 테스트로 전환하는 데 있다. 변경사항은 기능별 브랜치와 Git worktree로 분리하고, Pull Request와 GitHub Actions를 통해 백엔드·프런트엔드 테스트, 빌드, 의존성 및 민감정보 검사를 통과한 경우에만 `main`에 반영하였다. 이를 통해 팀 구성원의 개발 숙련도 차이를 보완하면서도 AI가 생성한 코드의 변경 이력과 검증 근거를 남길 수 있었다.

서비스 운영 단계에서는 먼저 생성형 AI에 의존하지 않는 결정론적 분석 기준선을 실행한다. 업로드된 PDF·DOCX·TXT 파일은 형식과 크기를 검사한 뒤 서버 내부에서 텍스트를 추출하고, 개인정보를 마스킹한 후 조항 단위로 분리한다. Rule Engine 0.5.0은 「약관의 규제에 관한 법률」 제6조부터 제11조 및 제14조를 법적 근거 후보로 참조하는 19개의 실험적 검토 유형을 적용해 `findings[]`를 생성한다. 이 결과는 생성형 AI가 생성·수정·삭제할 수 없다. 예를 들어 Test 10에서 확인된 정상 변경 조항의 오탐을 줄이기 위해 R04에는 ‘사전 개별 통지’, ‘고객의 실질적인 거절 또는 계약 종료권’, ‘변경의 장래 적용’이 위험 표현 주변에 모두 존재할 때만 작동하는 복합 예외 게이트를 추가하였다. 반대로 Test 11에서 놓쳤던 ‘계속 사용하면 새 조건에 동의’, ‘일정 시간 무응답이면 수용 확정’, ‘계속 거래하면 계약상 지위 이전을 승인’과 같은 표현은 R11 의미 패턴으로 보강하였다.

규칙 엔진이 놓칠 수 있는 우회 표현은 EC2 내부에서 구동하는 `intfloat/multilingual-e5-small`이 보완한다. 이 모델은 문장을 생성하는 생성형 AI가 아니라 문장의 의미적 유사도를 수치화하는 오프라인 임베딩 모델이다. 고정한 모델 리비전과 가중치 해시를 사용하며, 규칙이 이미 탐지한 유형과 중복되지 않는 후보만 `candidate_findings[]`에 추가한다. 따라서 사용자 화면에서도 결정론적으로 탐지된 ‘검토 신호’와 의미 유사도로 찾은 ‘추가 의미 검토 후보’가 분리된다. 의미 후보는 자동으로 결정론적 신호로 승격되지 않으며 최종적인 법률 판단으로도 사용되지 않는다.

의미 후보의 정밀도를 높이기 위해 법률 근거 검색과 분리된 내부 판단 RAG를 추가하였다. 별도의 ChromaDB `decision_cards` 컬렉션에는 테스트 계약서 원문이 아니라 일반화한 합성 위험 패턴과 정상 예외 패턴만 저장한다. 후보 조항마다 위험 카드와 예외 카드를 검색한 뒤 ‘벡터 유사도 60% + BM25 키워드 일치도 20% + 판단요소 충족도 20%’로 점수를 계산한다. 위험 점수가 0.70 이상이고 정상 예외와의 차이가 0.08 이상이며 핵심 판단요소가 두 개 이상 확인되면 내부적으로 위험 패턴 우세로 본다. 정상 예외 카드의 필수 요소가 모두 충족된 경우에만 해당 의미 후보를 사용자 결과에서 제외한다. 검색 모델이나 ChromaDB에 장애가 발생하거나 판단 자료가 부족하면 후보를 삭제하지 않고 기존 결과를 유지하는 fail-open 정책을 사용한다. 판단 카드, 검색 점수, 판정 상태와 카드 버전은 내부 평가와 원문 없는 운영 지표에서만 확인하며 사용자 화면·PDF·공개 API 또는 법적 근거에는 노출하지 않는다.

법률 근거 RAG는 내부 판단 RAG와 목적과 데이터가 다르다. ChromaDB에는 법령, 공정거래위원회 심결, 법원 판례, 분쟁조정 사례, 조항 패턴을 구분하는 5개 법률 컬렉션 구조가 구현되어 있다. 각 마스킹 조항을 질의로 사용하고 벡터 점수 45%, BM25 점수 40%, 출처 권위 점수 15%를 결합해 상위 3개의 근거 후보를 선택한다. 다만 현재 출처 URL, 기준일과 SHA-256 해시까지 검증하여 실제 적재한 공개 코퍼스는 「약관의 규제에 관한 법률」 7개 조문이다. 공정위 심결·법원 판례·분쟁조정 자료는 개별 출처와 라이선스, 재배포 가능성을 확인한 뒤 단계적으로 확대할 예정이다. 따라서 5개 컬렉션의 검색 구조가 구현되었다는 사실과 5개 종류의 공식 자료가 모두 확보되었다는 주장을 구분한다. 또한 합성 `decision_cards`는 공식 법률 자료와 별도 컬렉션에 저장하며 법적 근거 후보로 인용하지 않는다.

운영 단계의 생성형 AI는 OpenAI Responses API이며 기본 모델은 `gpt-5.6-luna`이다. 현재 OpenAI는 두 가지 선택적 역할을 수행한다. 첫째, 19개 규칙과 로컬 E5가 포착하지 못한 표현이 있는지 마스킹된 분석 조항을 문서 단위로 한 차례 검토하고, 폐쇄형 R01~R19 taxonomy 안에서 추가 의미 후보만 제안한다. 모델이 반환한 `section_id`와 `rule_id`가 실제 입력과 일치하고, `evidence_quote`가 해당 마스킹 조항에 존재하는 정확한 연속 문자열이며, 동일 조항·동일 유형의 기존 신호가 없는 경우에만 후보를 채택한다. 둘째, 확정된 검토 신호와 추가 의미 후보의 구조화된 요약 정보를 바탕으로 사용자가 먼저 읽을 핵심 문장 2~3개를 생성한다. `gpt-5.6-terra`는 복잡한 재검토를 위한 deep 등급 설정으로 남아 있지만 현재 기본 운영 흐름에서 자동 호출하지 않는다.

OpenAI의 역할은 법률 근거 검색을 대신하거나 계약의 적법성을 판정하는 것이 아니다. 조항별 상위 법률 근거의 검색·정렬·부착은 애플리케이션 코드가 결정론적으로 수행한다. 모든 Responses API 요청은 JSON Schema 기반 Structured Outputs로 제한하며, 출력 형식과 ID, 인용문, 금지된 법률 단정 표현을 다시 코드로 검사한다. 모델이 ‘위법하다’, ‘적법하다’, ‘무효이다’처럼 확정적인 결론을 포함하거나 허용되지 않은 ID와 인용문을 반환하면 결과를 채택하지 않는다. 즉, 생성형 AI는 검토 범위를 넓히고 설명을 읽기 쉽게 만드는 보조 계층이며, 규칙 결과와 공식 근거의 통제권은 애플리케이션에 남는다.

개인정보 보호도 외부 모델 호출 이전의 코드 기반 게이트로 구현하였다. 원본 파일 바이트와 마스킹 전 계약서 원문은 OpenAI로 전송하지 않는다. PII 마스킹을 통과한 최소 조항 또는 이미 구조화된 검토 결과만 전송하며, 외부 요청 직전 payload 전체에 대해 PII를 다시 검사한다. 모든 Responses API 요청에는 `store: false`를 지정하여 API 요청 저장 기능을 비활성화하였다. 이는 공급자의 모든 데이터 처리 가능성이 물리적으로 0이라는 의미가 아니므로, 보고서에서는 ‘OpenAI 서버에 데이터가 전혀 남지 않는다’고 단정하지 않는다. 외부 API 호출 실패, 호출 예산 소진, 스키마 오류 또는 검증 실패가 발생하면 결정론 결과는 그대로 보존하고, 요약은 기존 템플릿으로 대체하거나 결과 상태를 `needs_review`로 전환한다. 최종 결과 역시 법률적 유·무효 판정이 아니라 사용자가 확인해야 할 검토 신호와 질문으로 제공한다.

현재 전체 처리 흐름은 다음과 같다.

`문서 검증 → PII 마스킹 → 19개 규칙 → E5 의미 후보 → 내부 판단 RAG → 법률 근거 검색 → 선택적 OpenAI 문맥 검토·요약 → 사용자 검토`

### 성능 기준선과 재실행 결과

성능 평가는 사용자가 정의한 11개 합성 계약서와 조항별 Ground Truth를 사용한다. 정답 위험이 결정론 검토 신호로 탐지되면 FULL 1점, 의미 후보 또는 합리적인 인접 유형으로 탐지되면 PARTIAL 0.5점, 누락은 MISS 0점으로 계산하고 정상 조항 탐지는 FP로 별도 기록한다. 첨부된 신규 재실행 결과에서는 직전 실행과 신규 실행의 평균이 모두 90.3점으로 같았고, 11개 문서의 점수도 전부 동일했다. 신규 점수의 중앙값은 95점, 범위는 60~100점이며 5개 문서가 100점을 기록했다. 이는 성능 상승을 의미하기보다 동일 Ground Truth 기준의 전체 충족도가 유지되었다는 뜻이다. 표본이 11개의 합성 문서이고 평가축별 FULL/PARTIAL 가중치가 적용된 점수이므로 실제 계약서 전체에 대한 정확도로 일반화하지 않는다.

- 100점: Test 01(결정론 신호 7건·의미 후보 1건), Test 02(10건·1건), Test 04(7건·2건), Test 06(27건·0건), Test 09(12건·0건)이다. 명시적 위험 탐지는 안정적이지만 Test 06은 중복·과잉 신호를 별도로 점검해야 한다.
- 95점: Test 05(13건·3건)는 대부분의 위험을 탐지했으나 계속 이용에 따른 동의 간주가 의미 후보에 의존했다.
- 90점: Test 03(8건·2건)은 소송비용 전가가 후보 단계였고, Test 08(5건·5건)은 면책 우회 표현이 후보 단계였다.
- 88점: Test 07(7건·5건)은 수수료와 즉시상환 위험이 결정론 신호로 승격되지 않았다.
- 70점: Test 10(5건·4건)은 정상 변경 조항에서 발생한 R04 오탐이 핵심 정밀도 문제였다.
- 60점: Test 11(2건·9건)은 의미 후보 의존도가 높고 행위 기반 동의 간주 표현을 일부 놓쳤다.

결정론 규칙 신호 개수는 11개 문서 모두 직전 실행과 같았고 신규 실행에서 총 103건이었다. 반면 의미 후보는 총 29건에서 32건으로 10.3% 증가했으며, 11개 중 4개 문서(36.4%)에서 후보 개수가 변했다. Test 05는 2→3건, Test 06은 1→0건, Test 08은 3→5건, Test 11은 8→9건으로 바뀌었다. 특히 Test 11에서는 R11·R19 후보가 새로 생긴 동시에 기존 R15 비용전가 후보가 사라져, 후보 수 증가만으로 품질 향상을 판단할 수 없음을 확인하였다. Test 06에서는 Ground Truth 평가축 11개에 대해 규칙 신호가 27건 발생하여 재현율과 별개로 중복·과잉 신호를 측정할 필요도 드러났다. 이 결과를 바탕으로 결정론 규칙과 의미 후보의 성능 및 재현성을 분리 평가한다.

### 단계별 성능 실험 방안

동일한 마스킹·조항 분리 결과를 아래 네 실험군에 입력해 구성요소의 효과를 단계적으로 분리한다.

- 실험군 A: 19개 규칙만 실행하여 결정론 기준선의 precision·recall과 반복 재현성을 측정한다.
- 실험군 B: A에 로컬 E5를 추가하고 decision RAG와 OpenAI는 비활성화한다. 의미 후보가 추가한 recall과 false positive를 측정한다.
- 실험군 C: B에 내부 판단 RAG를 추가하고 OpenAI는 비활성화한다. 정상 예외 필터가 후보 precision과 recall에 미치는 효과를 측정한다.
- 실험군 D: C에 OpenAI 문맥 검토와 요약을 추가한다. 외부 모델의 추가 탐지 효과, 후보 변동성, 처리시간과 토큰 비용을 측정한다.

각 실험은 코드 커밋, Rule Engine 버전, E5 모델 리비전·가중치 해시, decision-card 버전, 법률 코퍼스 manifest, OpenAI 모델·prompt 버전을 고정하고 11개 문서를 실험군별 5회씩, 총 220회 분석한다. 비교 단위는 단순 후보 개수가 아니라 `section_id + rule_id` 집합으로 고정한다. 품질 지표는 규칙·후보별 precision, recall, F1과 정상 조항 FP 수를 사용하고, 기존 90.3점 방식은 직전 결과와의 연속성 확인용 보조 지표로만 유지한다. 반복 안정성은 첫 실행과 후속 실행의 후보 집합을 비교하여 Jaccard 유사도 `|교집합| / |합집합|`과 candidate churn rate `|대칭차집합| / |합집합|`을 계산한다. 여기에 결정론 신호 집합의 완전 일치율과 반복 실행 recall을 함께 기록한다. 운영 지표로는 문서별 처리시간의 중앙값과 95백분위수, OpenAI 입출력 토큰·호출 실패율, 내부 RAG가 제거한 후보 수와 판단 자료 부족률을 측정한다. 입력 조항과 모델 출력 원문은 성능 로그에 저장하지 않는다.

0.5.0의 1차 합격 조건은 다음과 같이 고정한다. Test 01~09의 결정론 `findings[]`는 기존 기준선과 완전히 같아야 하고, 5회 반복에서도 결정론 신호 집합 일치율이 100%여야 한다. Test 10의 정상 변경 조항은 R04 오탐이 0/5회여야 한다. Test 11의 계속 사용, 48시간 무응답, 계속 거래 기반 동의 표현은 세 유형 모두 매회 R11 의미 후보로 생성되어 반복 recall 100%를 충족해야 한다. C 실험군은 B보다 정상 조항 FP를 줄이면서 기대 후보 recall을 떨어뜨리지 않아야 하며, D 실험군의 의미 후보 Jaccard 유사도 목표는 0.85 이상, candidate churn rate 목표는 0.15 이하로 둔다. 내부 RAG 또는 OpenAI 장애를 강제로 발생시킨 실험에서는 분석 완료율 100%, 결정론 결과 보존율 100%, 마스킹 전 개인정보 외부 전송 0건을 확인한다. 목표 미달 후보는 결정론 신호로 승격하지 않고 계속 사용자 검토 후보로 유지한다.

첨부 재실행 결과는 위 보강 이전 기준선으로 사용한다. 이 결과에서 Test 10의 정상 변경 조항은 여전히 R04 FP였고, Test 11은 ‘계속 사용=동의’와 ‘계속 거래=이전 승인’을 놓쳤다. PR #18에서는 이 두 실패 원인을 겨냥해 R04 복합 예외 게이트와 R11 세 가지 의미 프로토타입, 내부 decision-card RAG를 구현하였다. 백엔드 테스트 140개와 프런트엔드 테스트 10개, 프런트엔드 빌드, 운영용 Docker Compose 설정, GitHub의 저장소 통합·의존성·히스토리 안전 검사는 모두 통과하였다. 다만 단위·통합 테스트 통과를 01~11 전체 성능 향상으로 바꾸어 표현하지 않는다. 0.5.0의 최종 수치는 EC2에 최신 `main`을 pull하고 Docker 이미지를 다시 빌드한 뒤 위 4개 실험군을 재실행하여 확정한다.

기존 EC2 운영 구성에서는 합성 계약서를 이용해 OpenAI Structured Outputs와 `store: false` 적용을 확인하고, ngrok HTTPS 주소에서 업로드, 분석, 결과 조회, PDF 생성과 문서 삭제까지의 흐름을 검증하였다. 새 내부 판단 RAG는 Git `main` 반영과 자동 테스트를 완료했지만 최신 Docker 이미지로 재배포하기 전까지는 EC2 운영 검증 완료로 표시하지 않는다. 최종 보고서에는 기준선 90.3점, 반복 안정성 지표, 0.5.0 재평가 수치와 실행 commit을 함께 기록하여 구현 변경과 성능 변화의 인과관계를 추적할 수 있게 한다.

### 현재 부족한 점과 향후 개선 방안

현재 시스템의 가장 큰 한계는 평가 범위와 의미 후보의 안정성이다. 성능 기준선은 11개의 합성 계약서에 한정되어 있어 실제 금융계약의 문체·길이·상품 다양성을 대표하지 못하고, 규칙과 합성 decision card도 아직 독립적인 법률 전문가 검토를 거치지 않았다. 결정론 규칙은 반복 실행에서 안정적이지만 의미 후보는 4개 문서에서 증감 또는 교체되어 동일 입력에 대한 재현성이 충분히 검증되지 않았다. Test 07의 수수료·즉시상환, Test 10의 자동갱신·24시간 무응답·전산기록, Test 11의 직원 과실 면책·정률 종료정산금처럼 위험 요소가 비교적 명확한 표현도 의미 후보에 머무는 경우가 있다. 반대로 Test 06처럼 Ground Truth 평가축보다 많은 신호가 생성되면 사용자가 같은 조항을 중복 검토해야 할 수 있다. 법률 근거도 현재 검증된 약관법 7개 조문으로 제한되어 있어 심결·판례·분쟁조정 사례에 근거한 설명력은 아직 향후 과제이다.

개선은 세 단계로 추진한다.

P0 단계에서는 EC2에 0.5.0 이미지를 재배포하고 A~D 실험군 220회 평가를 완료하여 Test 10의 R04 오탐 제거, Test 11의 R11 세 패턴 반복 탐지, 후보 Jaccard 0.85 이상을 우선 검증한다. 이때 임계값은 합성 개발셋에서만 조정하고 01~11 평가셋은 최종 회귀 확인에 사용하여 평가 데이터에 대한 과적합을 방지한다.

P1 단계에서는 반복 실험에서 안정적으로 재현되고 위험 요소가 명확한 후보만 별도의 복합 결정론 규칙으로 승격하며, Test 07·10·11의 미흡 유형을 우선 보강한다. 동일 조항의 여러 규칙은 내부적으로 개별 ID를 유지하되 사용자 화면에서는 조항 단위로 묶어 검토 부담을 줄이고, RAG 제거 수·자료 부족률·중복 신호율을 운영 지표로 관리한다.

P2 단계에서는 국가법령정보센터, 공정거래위원회, 법원 판례와 분쟁조정 자료를 출처·기준일·해시·라이선스 검증 후 순차적으로 적재하고, 규칙 작성에 참여하지 않은 법률 전문가의 블라인드 평가와 허가된 실제 문서 파일럿을 수행한다. 동시에 처리시간 P95, 문서당 OpenAI 토큰·비용, API 실패율과 개인정보 차단 건수를 관찰하여 품질 향상이 비용과 지연 증가를 정당화하는지 판단한다. 각 단계에서 합격 기준을 충족하지 못하면 기존 결정론 기준선으로 롤백하고, 최종 법률 판단은 계속 사용자의 검토에 맡긴다.
