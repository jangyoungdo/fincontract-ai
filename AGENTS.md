# FinContract AI 작업 규칙

이 파일은 사람과 코딩 에이전트가 같은 저장소에서 충돌 없이 작업하기 위한 프로젝트 규칙입니다.

## 작업 단위

- `main`에서 직접 개발하지 않습니다.
- 하나의 작업은 하나의 브랜치와 하나의 worktree를 사용합니다.
- 브랜치에는 한 가지 목적만 포함합니다.
- 다른 사람의 worktree 또는 브랜치에서 작업하지 않습니다.

## 변경 경계

- 사용자 계약서 원문, API 키, `.env`, 로컬 DB와 ChromaDB 데이터는 커밋하지 않습니다.
- `research/`에는 공개·허가 자료만 둡니다.
- 641건·16건 통계는 검증된 manifest가 없으면 확정 수치로 표시하지 않습니다.
- 마스킹 전 원문을 Claude API 또는 ChromaDB로 전달하는 변경은 허용하지 않습니다.
- 법률 결과는 위험 신호와 검토 필요성으로 표현하며 위법·적법을 확정하지 않습니다.

## 구현 원칙

- 빈 함수, TODO 중심 stub, 하드코딩된 분석 결과를 완료물로 제출하지 않습니다.
- 코드 변경에는 관련 테스트와 문서 변경을 함께 포함합니다.
- API, 규칙, prompt, corpus index의 버전을 추적합니다.
- unrelated formatting이나 대규모 파일 이동을 기능 변경과 섞지 않습니다.

## 커밋과 리뷰

- Conventional Commits 형식을 사용합니다.
- PR에는 변경 목적, 검증 결과, 개인정보·법률 경계 영향과 스크린샷을 기록합니다.
- 최소 한 명의 다른 작업자가 검토한 뒤 병합합니다.
- merge queue가 없으면 최신 `origin/main`을 반영하고 CI를 다시 통과한 뒤 squash merge합니다.

